# T013 — frame-skip-i320-apk-1.0.27

**Status final:** concluído
**Período:** _(ver TASK abaixo)_ → 2026-05-10
**Vinculado a:** _(ver TASK abaixo)_

---

## TASK (snapshot ao fechar)

# SEM TASK ATIVA — 2026-05-10

A TASK 13 (frame skip alternado + i320 → APK 1.0.27) foi **concluída ✅** e validada em campo pelo Maicon. App em produção. Fase de aceleração fechada.

**Não executar nada automaticamente.** Aguardar próxima TASK escrita por Cowork.

## Para contexto

- Última TASK concluída: ver `_handoff/RESULT.md`.
- Estado atual do projeto: ver `_handoff/STATE.md` e `PROJECT_STATE.md`.
- Backlog priorizado: ver `PROJECT_STATE.md` seção "Próximos Passos".
- Pesquisa arquivada sobre runtimes alternativos: `_research/2026-05-10_inference_runtimes_android.md`.
- Draft de TASK futura (Serviço Nativo Kotlin + ORT NNAPI, backlog Fase 2): `_research/draft_task14_native_service.md`.

## Próxima invocação do Claude Code

Será iniciada por Cowork escrevendo nova TASK aqui, em resposta a um pedido do Maicon. Triggers prováveis:
- Maicon reportar falso positivo / calibração necessária após uso de campo.
- NVMe Samsung 970 Pro chegar (previsto 18/05/2026) → migração de filesystem.
- Decisão de iniciar Fase 2 (re-treino unificado v4) pós-AM5.


---

## RESULT

# RESULT — TASK 13 (Frame skip alternado + i320 → APK 1.0.27)

**Data:** 2026-05-10
**Status:** APK gerado com sucesso — aguardando validação no S24.

---

## Passos 1-2 — Re-export i320 nodfl

- `coco_yolov8n_nc80_v0_i320_nodfl.onnx`: 12.1 MB
- `fulldet_yolov8n_nc95_v3_m692_i320_nodfl.onnx`: 12.7 MB
- Tempo de export: ~1 min cada

**Nota:** tamanho ONNX não depende do imgsz (só dos pesos). O ganho de velocidade vem da área de entrada menor (320² vs 416²), não do arquivo menor.

**Bug corrigido em `scripts/export_unified.py`:** DFL_PREFIXES insuficiente para modelos exportados com `simplify=False`. Nós `Shape`, `Gather`, `Add`, `Mul`, `Mul_1` permaneciam no grafo com inputs inválidos (referenciando nós DFL removidos), causando `InvalidArgument` no OnnxRuntime. Fix: ampliar DFL_PREFIXES para cobrir toda a cadeia de decode (19 → 24 nós removidos).

---

## Passo 3 — Sanity desktop

- N anchors confirmado: **2100** (40²+20²+10² = 1600+400+100) ✓
- Shape outputs:
  - COCO: `[1, 64, 2100]` (raw boxes) + `[1, 80, 2100]` (scores)
  - BR: `[1, 64, 2100]` (raw boxes) + `[1, 95, 2100]` (scores)
- bus.jpg detecções: 3 person + 1 bus (conf ≥ 0.25)

---

## Passo 4 — Refactor core/detection_engine.py

- `input_width/height = 320` ✓
- `_process_single_engine()` extraído (lógica compartilhada entre `detect()` e `detect_one()`)
- `detect_one(frame, engine_idx)` adicionado ✓
- Sanity de paridade desktop:
  - `detect()`: 5 detecções (3 person + 1 bus + 1 person conf baixo)
  - `detect_one(0)`: 5 detecções (COCO) ✓
  - `detect_one(1)`: 0 detecções (BR — correto, bus.jpg sem fauna BR)
  - União cobre `detect()`? ✓ sim

---

## Passo 5 — Refactor mobile/main.py

- `last_dets_by_model = [[], []]` adicionado ✓
- `_frame_count_ia = 0` adicionado ✓
- `ia_processing_loop` reescrito com `engine_idx = frame_count % 2` ✓
- Cache `last_dets_by_model[engine_idx]` + união como `last_detections` ✓

---

## Passos 6-8 — Edits paths + buildozer

- `test_webcam.py`: paths → i320 ✓
- `mobile/main.py` paths: → i320 ✓
- `buildozer.spec` 1.0.27 ✓
- `exclude_patterns`: i416 FP32 adicionados (saem do APK), i320 FP32 não estão na lista (entram no APK) ✓

---

## Passo 9 — Build APK 1.0.27

- **Arquivo:** `bin/animaldetector-1.0.27-arm64-v8a-debug.apk`
- **Tamanho:** 94 MB
- **Tempo:** ~26 min (compilação numpy do zero)
- **Erros:** nenhum — GREEN LIGHT

---

## Passo 10 — Validação S24

**Executado** — S24 conectado após build.

| Sinal | Resultado |
|---|---|
| `[DEBUG] Plataforma: ANDROID` | ✅ |
| 2 engines i320 carregadas (OpenCV CPU, nodfl) | ✅ |
| CV-PULSE câmera | ✅ ~28-30 fps |
| detect_one alternando COCO/BR | ✅ (shape 2100,84 e 2100,99) |
| max_score BR = 0.000 | ✅ esperado (eletrônicos não são fauna BR) |
| Detecções COCO visíveis no screenshot | ✅ NOTEBOOK 80.6%, TECLADO, MOUSE |
| FATAL EXCEPTION | ❌ nenhum |
| Traceback | ⚠️ Permission denied nos ícones do Kivy — não-fatal, app continua |

**Cadência de inferência medida:**
- Entre prints (cada um = 10 detect_one calls): ~1.0–1.4s
- **Taxa real: ~9 detect_one calls/segundo**

---

## Conclusão técnica

| Métrica | 1.0.26 | 1.0.27 | Melhora |
|---|---|---|---|
| IA FPS (detect_one/s) | ~1.67 | **~9** | **5.4×** |
| Câmera FPS | ~30 | ~28-30 | igual |
| COCO 80 classes | ✅ | ✅ | igual |
| Fauna BR 15 classes | ✅ | ✅ (cache alternado) | igual |
| Crash | — | nenhum | — |

- Meta da TASK era 5-7 fps percebido → **entregou ~9 fps** ✅
- Cobertura: COCO + BR mantidas, cada modelo atualiza a ~4.5 Hz
- Sem mudança de runtime — cv2.dnn FP32 OpenCV, mesma infra estável
- Rollback disponível: 1.0.26 em `bin/` (não apagar)

