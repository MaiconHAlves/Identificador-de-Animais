# Builds e caches
__pycache__/
*.pyc
.buildozer/
bin/
build/
.gradle/

# Logs grandes (já capturados em outros lugares)
*.log
!_agent/logs/*.log

# Screenshots de debug
emulator_*.png
native_camera_check*.png
s24_check.png

# Modelos pesados (não versionar; uso local)
models/*.onnx
models/*.tflite
*.pt
*.pth

# Datasets (referenciados, não versionados)
datasets/
fauna_br/
road-animals-2/
thermal-animal-1/

# Sandbox / IDE
.vscode/
.idea/
.claude/scheduled_tasks.lock

# OS
.DS_Store
Thumbs.db

# Outputs de treino YOLO (Ultralytics)
runs/

# Backups locais — não versionar (manter no disco se quiser)
*.bak
*.bak.*
*.bak_*
*.backup

# ONNX no root (gerados por scripts/exports; manter modelos só em models/ se tracked)
/*.onnx
yolov8n_saved_model/
yolov8n_int8.onnx
calibration_image_*.npy

# State files (mudam por execução)
.monitor_history.json
download_progress.json
loop_state.json

# Relatórios temporários
gpu_test_report.txt
system_report.txt
logcat_analysis.txt

# Pastas temp/scratch
tmp/

# Runtime do handoff (padrão meta — mode.txt + WAKE/PAUSE/META_INBOX)
_handoff/mode.txt
_handoff/META_INBOX.md
_handoff/META_INBOX.md.processed-*
_handoff/PAUSE.flag
_handoff/WAKE.md
_handoff/WAKE.md.processed-*
